---
  import Layout from "../layouts/Layout.svelte";
  import List from '../components/List.svelte';

  export interface ProjectType {
    id: string;
    name: string;
    description: string;
    topics?: string[];
    html_url?: string;
    homepage?: string;
  }

  const test = await fetch("https://api.github.com/users/floriankuc/repos?sort=updated")
  const res: ProjectType[] = await test.json()

  console.log(res)

  // const [value, setValue] = useState<ProjectType[]>([]);
  // const [isLoading, setIsLoading] = useState(false);
  // const [isSuccess, setIsSuccess] = useState(false);
  // const [error, setError] = useState<unknown>();

  // const getProjects = async (): Promise<void> => {
  //   setIsLoading(true);
  //   try {
  //     const { data, status } = await axios.get(
  //       "https://api.github.com/users/floriankuc/repos?sort=updated"
  //     );
  //     setValue(data);
  //     if (status === 200) setIsSuccess(true);
  //   } catch (error) {
  //     setError(error);
  //   }
  //   setIsLoading(false);
  // };

  // useEffect(() => {
  //   getProjects();
  // }, []);

---

<Layout client:load><List items={res}/></Layout>






